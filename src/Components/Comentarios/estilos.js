import { StyleSheet, Dimensions } from 'react-native'

const estilo = StyleSheet.create({
  inline: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 5
  },
  imgSend: {
    width: 30,
    height: 30,
    marginRight: 10,
    opacity: 0.8
  },
  espaçamento: {
    marginRight:10
  }
});

export default estilo;